<section id="contacto" class="contact-section">
    <div class="contact-container">
        <!-- Encabezado Editorial Consistente -->
        <header class="contact-header reveal">
            <div class="line-reveal">
                <span class="index">06</span>
                <span class="label">Contacto</span>
            </div>
            <h2 class="main-title">
                Empecemos algo <br />
                <span class="italic">único</span>.
            </h2>
        </header>

        <div class="contact-grid">
            <!-- Info Column -->
            <div class="contact-info reveal" style="transition-delay: 150ms">
                <p class="lead-text">
                    ¿Tienes un proyecto en mente? Estoy disponible para nuevos
                    desafíos globales. Hablemos de cómo podemos colaborar.
                </p>

                <div class="info-blocks">
                    <div class="info-item">
                        <span class="info-tag">Email</span>
                        <a
                            href="mailto:daniel.ochoaa.cass@gmail.com"
                            class="info-value">daniel.ochoaa.cass@gmail.com</a
                        >
                    </div>

                    <div class="info-item">
                        <span class="info-tag">Social</span>
                        <div class="social-grid">
                            <a
                                href="https://www.linkedin.com/in/danielochoaa/"
                                target="_blank"
                                class="social-pill">LinkedIn</a
                            >
                        </div>
                    </div>
                </div>
            </div>

            <!-- Form Column -->
            <div
                class="contact-form-wrapper reveal"
                style="transition-delay: 300ms"
            >
                <form
                    class="contact-form"
                    id="contactForm"
                    action="https://formspree.io/f/mqezoyok"
                    method="POST"
                >
                    <div class="form-row">
                        <div class="form-group">
                            <input
                                type="text"
                                id="name"
                                name="name"
                                required
                                placeholder=" "
                                class="form-input"
                            />
                            <label for="name" class="form-label">Nombre</label>
                        </div>
                        <div class="form-group">
                            <input
                                type="email"
                                id="email"
                                name="email"
                                required
                                placeholder=" "
                                class="form-input"
                            />
                            <label for="email" class="form-label">Email</label>
                        </div>
                    </div>

                    <div class="form-group">
                        <textarea
                            id="message"
                            name="message"
                            required
                            placeholder=" "
                            class="form-input form-textarea"
                            rows="4"></textarea>
                        <label for="message" class="form-label">Mensaje</label>
                    </div>

                    <button type="submit" class="submit-btn group">
                        <span class="btn-text">Enviar Mensaje</span>
                        <div class="btn-circle">
                            <svg
                                width="20"
                                height="20"
                                viewBox="0 0 24 24"
                                fill="none"
                                stroke="currentColor"
                                stroke-width="2.5"
                            >
                                <path d="M5 12h14M12 5l7 7-7 7"></path>
                            </svg>
                        </div>
                    </button>
                </form>
            </div>
        </div>
    </div>
</section>

<style>
    .contact-section {
        padding: 160px 5%;
        background-color: var(--bg-main);
    }

    .contact-container {
        max-width: 1400px;
        margin: 0 auto;
    }

    /* Editorial Header matching previous sections */
    .contact-header {
        margin-bottom: 100px;
    }

    .line-reveal {
        display: flex;
        align-items: center;
        gap: 20px;
        margin-bottom: 2rem;
    }

    .index {
        font-family: var(--font-main);
        font-size: 0.9rem;
        font-weight: 800;
        color: var(--accent-dark);
        opacity: 0.5;
    }

    .label {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 4px;
        font-weight: 700;
        color: var(--texto-muted);
    }

    .main-title {
        font-family: var(--font-heading);
        font-size: clamp(3rem, 8vw, 6rem);
        line-height: 0.95;
        font-weight: 900;
        color: var(--texto);
        letter-spacing: -0.02em;
    }

    .main-title .italic {
        font-style: italic;
        color: var(--accent-dark);
        font-weight: 400;
    }

    /* Layout */
    .contact-grid {
        display: grid;
        grid-template-columns: 1fr 1.2fr;
        gap: 120px;
    }

    .lead-text {
        font-family: var(--font-heading);
        font-size: 2rem;
        font-weight: 700;
        line-height: 1.2;
        color: var(--texto);
        margin-bottom: 4rem;
    }

    .info-blocks {
        display: flex;
        flex-direction: column;
        gap: 3rem;
    }

    .info-tag {
        display: block;
        font-size: 0.7rem;
        text-transform: uppercase;
        letter-spacing: 2px;
        font-weight: 800;
        color: var(--accent-dark);
        margin-bottom: 1rem;
    }

    .info-value {
        font-family: var(--font-heading);
        font-size: clamp(1.2rem, 2.5vw, 1.8rem);
        color: var(--texto);
        text-decoration: none;
        font-weight: 700;
        transition: opacity 0.3s ease;
        word-break: break-all;
    }

    .info-value:hover {
        opacity: 0.5;
    }

    .social-grid {
        display: flex;
        flex-wrap: wrap;
        gap: 14px;
    }

    .social-pill {
        padding: 10px 24px;
        border: 1px solid var(--glass-border);
        border-radius: 100px;
        text-decoration: none;
        color: var(--texto);
        font-size: 0.9rem;
        font-weight: 700;
        transition: all 0.3s ease;
    }

    .social-pill:hover {
        background: var(--texto);
        color: var(--bg-main);
        border-color: var(--texto);
    }

    /* Form Styles - Editorial & Clean */
    .contact-form {
        display: flex;
        flex-direction: column;
        gap: 4rem;
    }

    .form-row {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 40px;
    }

    .form-group {
        position: relative;
    }

    .form-input {
        width: 100%;
        background: transparent;
        border: none;
        border-bottom: 1px solid var(--glass-border);
        padding: 1rem 0;
        font-family: var(--font-main);
        font-size: 1.1rem;
        color: var(--texto);
        outline: none;
        transition: border-color 0.4s ease;
        border-radius: 0;
    }

    .form-textarea {
        resize: none;
        min-height: 120px;
    }

    .form-label {
        position: absolute;
        top: 1rem;
        left: 0;
        font-size: 1.1rem;
        color: var(--texto-muted);
        pointer-events: none;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        transform-origin: left top;
    }

    .form-input:focus,
    .form-input:not(:placeholder-shown) {
        border-color: var(--accent-dark);
    }

    .form-input:focus + .form-label,
    .form-input:not(:placeholder-shown) + .form-label {
        transform: translateY(-2.2rem) scale(0.7);
        color: var(--accent-dark);
        font-weight: 800;
        letter-spacing: 1px;
    }

    .submit-btn {
        margin-top: 2rem;
        align-self: flex-start;
        display: flex;
        align-items: center;
        gap: 1.5rem;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0;
        color: var(--texto);
    }

    .btn-text {
        font-size: 0.9rem;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 3px;
    }

    .btn-circle {
        width: 60px;
        height: 60px;
        border: 1px solid var(--texto);
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .submit-btn:hover .btn-circle {
        background: var(--texto);
        color: var(--bg-main);
        transform: rotate(-45deg);
    }

    /* Animation Reveal */
    .reveal {
        opacity: 0;
        transform: translateY(30px);
        transition: all 1s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .reveal.active {
        opacity: 1;
        transform: translateY(0);
    }

    @media (max-width: 1024px) {
        .contact-grid {
            grid-template-columns: 1fr;
            gap: 80px;
        }
        .lead-text {
            margin-bottom: 2rem;
        }
    }

    @media (max-width: 600px) {
        .form-row {
            grid-template-columns: 1fr;
            gap: 40px;
        }
    }
</style>

<script>
    const form = document.getElementById("contactForm") as HTMLFormElement;

    if (form) {
        form.addEventListener("submit", async (e) => {
            e.preventDefault();
            const btn = form.querySelector(".submit-btn") as HTMLButtonElement;
            const btnText = btn.querySelector(".btn-text");
            if (btnText) btnText.textContent = "Enviando...";

            try {
                const formData = new FormData(form);
                const response = await fetch(form.action, {
                    method: form.method,
                    body: JSON.stringify(Object.fromEntries(formData)),
                    headers: {
                        "Content-Type": "application/json",
                        Accept: "application/json",
                    },
                });

                if (response.ok) {
                    if (btnText) btnText.textContent = "¡Enviado!";
                    form.reset();
                } else {
                    if (btnText) btnText.textContent = "Error";
                }
            } catch (error) {
                if (btnText) btnText.textContent = "Error";
            } finally {
                setTimeout(() => {
                    if (btnText) btnText.textContent = "Enviar Mensaje";
                }, 4000);
            }
        });
    }
</script>
